package org.example.day15.문제;

import java.util.ArrayList;

public class Q3_Cast {
    public static void main(String[] args) {
//        // ArrayList는 제네릭 타입을 사용하지 않았기 때문에 다양한 타입을 담을 수 있음
//        ArrayList list = new ArrayList();
//
//        // String 타입이므로 객체로 자동으로 추가 가능 (Object로 자동 형변환)
//        list.add("자바");  // 자동형변환: String -> Object
//
//        // 새로운 사람() 객체를 추가 (사람 클래스는 Object의 서브클래스)
//        list.add(new 사람());  // 자동형변환: 사람 -> Object
//
//        // 새로운 ArrayList 객체를 추가
//        list.add(new ArrayList());  // 자동형변환: ArrayList -> Object
//
//        // get(0)은 Object 타입을 반환하므로 이를 String으로 강제 형변환해야 함
//        String s = (String)list.get(0);  // 강제형변환 (캐스팅): Object -> String
//
//        // 정수 100을 추가
//        list.add(100);  // 자동박싱: 기본형 int -> Integer 객체
//
//        // get(3)은 Integer 객체를 반환하므로, 이를 int로 변환해야 함 (언박싱)
//        int i = (Integer)list.get(3);  // 언박싱: Integer -> int
    }

}
